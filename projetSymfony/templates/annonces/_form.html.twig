{{ form_start(form) }}
    {{ form_row(form.categorie , {'attr' : {'class' : 'form-control'}}) }}
    {{ form_row(form.titre,{'attr' : {'class' : 'form form-control'}} ) }}
    {{ form_row(form.description,{'attr' : {'class' : 'form form-control',
                    'style':' height:150px '}} ) }}
    {{ form_row(form.prix,{'attr' : {'class' : 'form form-control'}} ) }}
    {{ form_row(form.imageFile,{'attr' : {'class' : 'form form-control'}} ) }}
    <br>
    <button class="btn text-white mt-2 pull-right"style="width:150px; height:40px; border-radius: 0px; ">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
